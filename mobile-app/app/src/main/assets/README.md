# App assets

| File | Where it comes from | Who |
|---|---|---|
| `species_classifier.tflite` | Google Drive `IsdaYou/results_run1/efficientnet_lite0/seed_42/model_fp16.tflite`, renamed. **Not in Git history yet — copy it here.** | Troy |
| `labels.txt` | Same Drive folder. The copy here was written from the manuscript list; replace it with the Drive file and check that nothing changed (line order = model output order). | Troy |
| `species.json` | The 31-species library. `index` = line number in labels.txt (0 = Bangus … 30 = Tilapia). Fill in the empty fields. | Diana (content from Jake and Franc) |
